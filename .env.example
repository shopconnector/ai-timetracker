# TimeTracker Configuration
# Skopiuj ten plik do apps/web/.env.local i wypełnij wartościami

# =============================================================================
# ACTIVITYWATCH
# =============================================================================
# URL do ActivityWatch API (domyślnie localhost:5600)
ACTIVITYWATCH_URL=http://localhost:5600

# =============================================================================
# TEMPO API (wymagane)
# =============================================================================
# Personal token z Tempo.
# Utwórz: Tempo → Settings → API Integration → Create token
TEMPO_API_TOKEN=

# =============================================================================
# JIRA API (wymagane)
# =============================================================================
# Base URL Twojej instancji Jira
JIRA_BASE_URL=https://your-company.atlassian.net

# Email konta serwisowego lub Twój email
JIRA_SERVICE_EMAIL=your.email@company.com

# Token API do Jira
# Utwórz: https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_API_KEY=

# =============================================================================
# ATLASSIAN OAuth 2.0 (3LO + PKCE) — alternatywa dla JIRA_API_KEY + Confluence R/W
# =============================================================================
# Jeśli skonfigurowane, OAuth ma priorytet nad Basic Auth (Jira) i odblokowuje
# Confluence (read + write). PKCE flow — Client Secret nie jest potrzebny.
#
# Setup (jednorazowo):
#   1. https://developer.atlassian.com/console/myapps/ → Create app → OAuth 2.0 integration
#   2. Permissions → Jira API + Confluence API + User identity API → Configure → scope'y:
#      read:me, read:jira-work, write:jira-work,
#      read:confluence-space.summary, read:confluence-content.summary,
#      read:confluence-content.all, write:confluence-content, read:confluence-user
#   3. Authorization → Callback URL: http://localhost:5666/timetracker/api/auth/atlassian/callback
#   4. Skopiuj Client ID → wpisz w UI Settings → Atlassian OAuth 2.0 → kliknij Connect
ATLASSIAN_OAUTH_CLIENT_ID=
ATLASSIAN_OAUTH_SITE_URL=https://your-company.atlassian.net
ATLASSIAN_OAUTH_REDIRECT_URI=http://localhost:5666/timetracker/api/auth/atlassian/callback
# Poniższe ustawiane AUTOMATYCZNIE przez callback handler po pierwszym Connect:
ATLASSIAN_OAUTH_ACCESS_TOKEN=
ATLASSIAN_OAUTH_REFRESH_TOKEN=
ATLASSIAN_OAUTH_EXPIRES_AT=
ATLASSIAN_OAUTH_CLOUD_ID=
ATLASSIAN_OAUTH_USER_EMAIL=
ATLASSIAN_OAUTH_USER_NAME=
ATLASSIAN_OAUTH_SCOPES=

# =============================================================================
# GEMINI API (zalecane — darmowe)
# =============================================================================
# Klucz API do Google Gemini — darmowy tier
# Utwórz: https://aistudio.google.com/apikey
# Domyślny model: gemini-2.5-flash (szybki, darmowy)
GEMINI_API_KEY=

# Opcjonalnie: zmien model (gemini-2.5-flash | gemini-2.5-pro | gemini-2.0-flash)
# GEMINI_MODEL=gemini-2.5-flash

# =============================================================================
# SLACK OAuth 2.0 (PKCE) — zalecane, bez Client Secret
# =============================================================================
# Slack OAuth v2 z PKCE — embed tylko Client ID, brak Client Secret w bundle.
# Token rotation forced dla desktop+PKCE (12h access, refresh single-use).
#
# Setup (jednorazowo):
#   1. https://api.slack.com/apps → Create New App → From scratch → workspace
#   2. OAuth & Permissions → Redirect URLs: http://localhost:5666/timetracker/api/auth/slack/callback
#   3. User Token Scopes (NIE Bot scopes): channels:history, channels:read,
#      groups:history, groups:read, im:history, im:read, mpim:history, mpim:read,
#      users:read
#   4. Advanced Token Security via Token Rotation → Opt In
#   5. Manage Distribution → Activate Public Distribution (wymagane dla install)
#   6. Basic Information → skopiuj Client ID → wpisz poniżej
SLACK_OAUTH_CLIENT_ID=
# Slack Distributed apps require client_secret in token exchange (even with PKCE)
SLACK_OAUTH_CLIENT_SECRET=
SLACK_OAUTH_REDIRECT_URI=http://localhost:5666/timetracker/api/auth/slack/callback
# Poniższe ustawiane AUTOMATYCZNIE przez callback handler — nie ruszaj ręcznie:
SLACK_OAUTH_ACCESS_TOKEN=
SLACK_OAUTH_REFRESH_TOKEN=
SLACK_OAUTH_EXPIRES_AT=
SLACK_OAUTH_USER_ID=
SLACK_OAUTH_TEAM_ID=
SLACK_OAUTH_TEAM_NAME=
SLACK_OAUTH_SCOPES=

# =============================================================================
# SLACK API (legacy — alternatywa dla OAuth)
# =============================================================================
# User Token (xoxp-) — widzi DM, kanaly, huddle
# Utwórz: https://api.slack.com/apps → OAuth & Permissions → Install to Workspace
# Wymagane scopes: channels:history, channels:read, groups:history, groups:read,
#   im:history, im:read, mpim:history, mpim:read, users:read
SLACK_USER_TOKEN=

# =============================================================================
# OPENROUTER (opcjonalne — fallback jesli brak Gemini)
# =============================================================================
# Token do OpenRouter jako zapasowy provider AI
# Utwórz: https://openrouter.ai/keys
OPENROUTER_API_KEY=
