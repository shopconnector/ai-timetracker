{
  "name": "timetracker-launcher",
  "version": "0.1.0",
  "description": "AI TimeTracker - Self-extracting launcher",
  "main": "index.js",
  "bin": "index.js",
  "scripts": {
    "build": "pkg . --target node20-win-x64 --output ../dist/TimeTracker.exe"
  },
  "pkg": {
    "targets": ["node20-win-x64"],
    "assets": ["assets/**/*"],
    "outputPath": "../dist"
  },
  "devDependencies": {
    "@yao-pkg/pkg": "^6.0.0"
  }
}
